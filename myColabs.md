# My Collaborations

## English

- Implemented date-range filtering with facets reference (`/api/metrics` + `/api/metrics/facets`).
- Added anomaly alerts table with threshold validation and empty/error/loading states (`/api/metrics/alerts`).
- Added B2B vs B2C comparison view with top income categories and comparative chart (`/api/metrics/categories/top`, `/api/metrics/facets`).
- Created frontend technical specs for API types, query param types, components, and feature contracts.

## Espanol

- Implemente el filtro por rango de fechas con referencia de facetas (`/api/metrics` + `/api/metrics/facets`).
- Agregue la tabla de alertas de anomalias con validacion de umbral y estados de vacio/error/carga (`/api/metrics/alerts`).
- Agregue la vista comparativa B2B vs B2C con top de categorias de ingreso y grafico comparativo (`/api/metrics/categories/top`, `/api/metrics/facets`).
- Cree especificaciones tecnicas frontend para tipos API, tipos de parametros, componentes y contratos funcionales.